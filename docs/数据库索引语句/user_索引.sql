-- ============================================
-- 用户表索引
-- ============================================

-- 主键索引：id（建表时自动创建）
-- ALTER TABLE `user` ADD PRIMARY KEY (id);

-- 唯一索引：手机号不能重复（建表时已包含）
-- ALTER TABLE `user` ADD UNIQUE KEY uk_phone (phone);

-- 说明：
-- 用户表数据量通常较小，当前索引已满足需求
-- phone 字段的唯一索引同时充当按手机号查询的加速索引
