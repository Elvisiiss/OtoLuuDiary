<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.otoluudiary.mapper.TodoMediaMapper">

    <resultMap id="mediaMap" type="com.otoluudiary.model.TodoMedia">
        <id property="id" column="id"/>
        <result property="todoId" column="todo_id"/>
        <result property="fileName" column="file_name"/>
        <result property="filePath" column="file_path"/>
        <result property="fileType" column="file_type"/>
        <result property="fileSize" column="file_size"/>
        <result property="createdAt" column="created_at"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <select id="findByTodoId" resultMap="mediaMap">
        SELECT * FROM todo_media WHERE todo_id = #{todoId} AND is_deleted = 0 ORDER BY created_at ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO todo_media (todo_id, file_name, file_path, file_type, file_size, created_at)
        VALUES (#{todoId}, #{fileName}, #{filePath}, #{fileType}, #{fileSize}, #{createdAt})
    </insert>

    <!-- 假删除：标记 is_deleted=1 -->
    <update id="deleteById">
        UPDATE todo_media SET is_deleted = 1, deleted_at = #{deletedAt}
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <!-- 假删除：按待做ID批量标记 -->
    <update id="deleteByTodoId">
        UPDATE todo_media SET is_deleted = 1, deleted_at = #{deletedAt}
        WHERE todo_id = #{todoId} AND is_deleted = 0
    </update>
</mapper>
